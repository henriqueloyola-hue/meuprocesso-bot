[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "nss", "nspr", "glib", "gtk3", "alsa-lib"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[start]
cmd = "python main.py"
